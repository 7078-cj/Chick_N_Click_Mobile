/** Laravel API origin (no trailing slash). */
export const API_BASE_URL = process.env.EXPO_PUBLIC_API_URL ?? "";

/** FastAPI / websocket base (no trailing slash). */
export const WS_BASE_URL = process.env.EXPO_PUBLIC_WS_URL ?? "";
